# Ferramentas

Antecedentes Criminais: SP expõe 1 ferramenta (todas somente leitura).

### 1. `antecedentes_criminais_sp_consultar`
**Input**: `nome`, `birthdate`, `genero`, `rg` (opcional), `rg_digito` (opcional), `rg_expedicao` (opcional), `cin_cpf` (opcional), `cin_expedicao` (opcional), `pai` (opcional), `mae` (opcional)

Antecedentes Criminais: SP, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar Antecedentes Criminais: SP
```
